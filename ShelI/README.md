# ShelI 脚本库

用于存放一些常用的脚本。

## 使用方法

```bash
# Debian 13 初始化脚本，通过命令行参数传入 SSH 公钥
curl -fsSL https://raw.githubusercontent.com/NEANC/PKB/main/ShelI/Debian13_Initialization.sh  | sudo bash -s -- -ssh <key.pub>
```
